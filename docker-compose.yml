services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: aacfl
      POSTGRES_PASSWORD: aacfl_local_only
      POSTGRES_DB: aacfl_dev
    ports:
      - '5432:5432'
    volumes:
      - postgres-data:/var/lib/postgresql/data
volumes:
  postgres-data:
