APP_URL="http://localhost:3000"
DATABASE_URL="postgresql://aacfl:aacfl@localhost:5432/aacfl_dev?schema=public"
AUTH_SECRET="replace-with-at-least-32-random-characters"
AUTH_TRUST_HOST="true"
AUTH_GITHUB_ID=""
AUTH_GITHUB_SECRET=""
# Google sign-in — fill both in to turn on "Sign in with Google".
# Create these at https://console.cloud.google.com (see docs/SETUP_LOGIN.md).
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""
# Your email. Whoever signs in with this address first becomes the owner
# automatically (no command line). Set it before your first sign-in.
INITIAL_OWNER_EMAIL=""
LOG_LEVEL="info"
TEST_ENV="false"
INITIAL_OWNER_EMAIL=""
# Optional. Free at https://pokemontcg.io — raises the live pricing search
# rate limit. Search works without it, just at a lower rate limit.
POKEMON_TCG_API_KEY=""
